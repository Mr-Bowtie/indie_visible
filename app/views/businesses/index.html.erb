<p style="color: green"><%= notice %></p>

<div class="container">
  <div class="is-flex is-justify-content-center mb-6"><h1 class="title is-1">Bookish Stores</h1></div>

  <div id="businesses" class="tile is-ancestor is-flex-wrap-wrap">
    <% @businesses.each do |business| %>
      <%= render business %>
    <% end %>
  </div>
</div>
